# TRAY DIGITA PLUGIN INSTALLER

add new tab on plugins page with `install` or `activate`

Update repository using update callback.


## USAGE

```php
add_action('admin_init', [Handler::instance(), 'init']);
```
